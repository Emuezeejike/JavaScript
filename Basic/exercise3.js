// VOTING ELIGIBILITY CHECKER

// request for the user name. Though its not i mportant to the assignment
let name = prompt('Please Enter your Name');

// Request for the user's Age
let age = prompt('Enter your Age');

// check if the user is 18 and avove or not. if 18 and above, alert "Eligible" while "Not eligible" if user is 17 and below
if (age >= 18) {
    alert('Eligible');
} else {
    alert('Not Eligible');
}